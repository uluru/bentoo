<h1><%= title %></h1>
<ul>
<%= items %>
<% items.forEach(function(item) { %>
  <li>
  <a href="/roomView?id=<%= item.account_id %>"><%= item.account_id %></a> / <%= item.name %> / 
  </li>
<% }) %>
</ul>
